const SeatTypes = (props) => {
    // get all the types on the system that are used on seats in a given screen

    return (
        <>
            test
        </>
    )
}

export default SeatTypes;
